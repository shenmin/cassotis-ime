# Cassotis IME（言泉输入法）

<p align="center">
  <img src="cassotis_ime_yanquan.png" alt="Cassotis IME logo" width="280">
</p>

<p align="center">
  <a href="LICENSE"><img src="https://img.shields.io/badge/license-GPL--3.0-blue" alt="License: GPL-3.0"></a>
</p>
<p align="center">
  <img src="snapshot.png" alt="Cassotis IME snapshot" width="550">
</p>

[English](README.md) | 简体中文

Cassotis IME（言泉输入法）是一个面向 Windows 10/11 的实验性中文拼音输入法项目，主要使用 Delphi，并基于 TSF（Text Services Framework）实现。

## 名称来源
英文名 **Cassotis** 源自 Delphi 神庙内的一眼圣泉。传说女祭司皮媞亚（Pythia）在发布神谕前会饮用此泉水，以进入通灵状态。这眼泉水被视为预言与灵感的真正源头，也是神谕诞生之地，呼应了从 Delphi 到人类语言的路径。

中文名 **言泉** 既契合 Cassotis 作为预言之泉的意象，也取“言如泉涌”的寓意，寄托了对流畅、智能输入体验的期许。

项目当前重点：
- 建立稳定的 TSF 输入法基础；
- 保持模块化架构（TSF DLL + host 进程 + 工具链）；
- 持续改进语料训练驱动的长句排序与短词上下文排序。

## 当前状态
- TSF 文本服务主流程已可用（注册、激活、组合生命周期）。
- TSF DLL 同时支持 Win64 与 Win32（`svr.dll` / `svr32.dll`），host 进程仅保留 Win64。
- 已实现候选窗、翻页、选词与上屏流程。
- 支持全拼及四种可选双拼方案：微软双拼、小鹤双拼、自然码双拼和搜狗双拼；所有方案共用候选排序与用户学习数据。
- 支持词库分离：简体基础库、繁体基础库、用户词库。
- 已集成可部署的本地神经残差重排器，在不影响短词精确匹配模式的前提下，保守修正完整长句候选排序。
- 已集成独立的短词上下文重排器，根据光标前已经上屏的文本处理存在歧义的精确候选；没有可用上下文时保持原有排序。
- 已实现上下文同步（surrounding text）与按键状态同步。

## 架构
- `src/tsf`：TSF COM 进程内服务（文本服务接入层）。
- `src/engine`：拼音解析、候选生成、排序与用户学习。
- `src/host`：外部 host 进程，负责引擎/UI 协同。
- `src/ui`：候选窗与托盘 UI。
- `src/common`：配置、日志、IPC、sqlite 封装与公共工具。
- `tools`：注册、词库构建/导入/诊断等工具程序。

## 仓库结构
- `src/` 源码
- `tools/` 工具工程与辅助可执行程序
- `data/` schema 与词表源数据
- `out/` 构建/注册/重建/测试脚本
- `third_party/` 第三方二进制/源码（例如 sqlite 运行库包）

## 关键二进制
- `cassotis_ime_svr.dll`（Win64 TSF 进程内 COM 服务）
- `cassotis_ime_svr32.dll`（Win32 TSF 进程内 COM 服务）
- `cassotis_ime_host.exe`（Win64 host 进程）
- `cassotis_ime_tray_host.exe`（Win64 托盘/状态宿主，负责托盘菜单、浮动状态窗口和输入状态显示）
- `cassotis_ime_profile_reg.exe`（TSF profile/category 注册工具）

若缺少 TSF DLL 或主 host 进程，输入法无法正常工作。若缺少托盘/状态宿主，核心输入路径可能仍可运行，但托盘菜单、浮动状态窗口和状态显示不可用。

## 构建与运行（快速开始）
前置要求：
- Windows 10/11
- Delphi 10.4
- SQLite 运行库（`sqlite3_64.dll`）

在 `out/` 目录执行：

```powershell
.\rebuild_all.ps1
.\cassotis_ime_profile_reg.exe register_tsf -dll_path .\cassotis_ime_svr.dll
.\rebuild_dict.ps1
```

完整构建说明见 `BUILD.md`。

## 词库流程
当前基础词库流程从 [cassotis-lexicon](https://github.com/shenmin/cassotis-lexicon) 项目导入生成产物：
- 导入输入文件：`dict_unihan_sc.txt`、`dict_unihan_tc.txt`、`dict_clean_sc.txt`、`dict_clean_tc.txt`
- 运行时数据库重建到 `%LOCALAPPDATA%\CassotisIme\data\`（如 `dict_sc.db`、`dict_tc.db`）
- 用户词库默认位于 `%LOCALAPPDATA%\CassotisIme\data\user_dict.db`

词库重建入口：

```powershell
.\rebuild_dict.ps1
```

## 语料训练增强的本地排序
Cassotis v1.1.0 引入离线训练的本地统计语言模型，用于改进长句路径排序。训练流程从清洗后的中文通用语料和小说语料中，学习受词库约束的词级二元、三元转移关系，以及带平滑回退的字符级三元语言模型。“长句基准测试-16300”语料与训练数据保持隔离，不参与模型训练。

Cassotis v1.3.0 新增项目首个可部署的长句神经残差重排器。这个紧凑的前馈神经模型基于受词库约束的 N-best 候选对比数据离线训练，保守提升更合理的完整长句候选，同时保留原始引擎结果作为回退。

Cassotis v1.4.0 将同样的离线训练方法扩展到短词输入。独立的上下文重排器会结合字符 LM 信号与光标前已经上屏的文本，对同一拼音下的精确候选进行比较。它只在存在左侧上下文且有多个精确候选竞争时参与排序；没有可用上下文时，短词候选仍保持原有顺序。

Cassotis v1.5.0 将短词上下文排序扩展为两阶段本地神经重排。第一阶段从竞争的精确候选中选择更符合前文的结果，第二阶段由独立训练的残差模型进行保守修正，只在分数优势超过门槛时改变排序。无上下文的短词输入仍不进入这条模型路径。

统计模型经过量化后写入本地词库数据库，紧凑的重排器则导出为确定性的原生 Pascal 参数。运行时只执行本地、规模受控的打分，不会启动 PyTorch/ONNX 环境或外部模型服务，也不需要联网或 GPU。长句与短词使用彼此独立的排序路径，一条路径的改进不会替代另一条路径的匹配规则。

## 长句基准测试-16300
测试方法、语料来源和评分规则见 [BENCHMARK.CN.md](BENCHMARK.CN.md)。

语料：开发者自己的小说著作 [**《永恒的舞动》**](https://www.qidian.com/book/1037259117/) 中 16,300 条符合条件的中文句子。

| 版本 | Top1 | Top2 | Mean (ms) | P50 (ms) | P95 (ms) | Max (ms) |
|---|---:|---:|---:|---:|---:|---:|
| `v1.4.0` | 7168/16300 (43.98%) | 7617/16300 (46.73%) | 66.23 | 46 | 218 | 2578 |
| `v1.3.0` | 7155/16300 (43.90%) | 7601/16300 (46.63%) | 64.54 | 46 | 203 | 2188 |
| `v1.2.0` | 6895/16300 (42.30%) | 7303/16300 (44.80%) | 59.89 | 32 | 188 | 2078 |
| `v1.1.0` | 6677/16300 (40.96%) | 7067/16300 (43.36%) | 73.18 | 47 | 234 | 2750 |
| `v1.0.0` | 6106/16300 (37.46%) | 6857/16300 (42.07%) | 71.49 | 47 | 219 | 5344 |
| `v0.8.5` | 6097/16300 (37.40%) | 6847/16300 (42.01%) | 520.05 | 406 | 1203 | 13297 |
| `v0.7.0` | 5368/16300 (32.93%) | 6110/16300 (37.48%) | — | — | — | — |
| `v0.6.0` | 4905/16300 (30.09%) | 5378/16300 (32.99%) | — | — | — | — |
| `v0.5.0` | 4834/16300 (29.66%) | 5243/16300 (32.17%) | — | — | — | — |
| `v0.4.0` | 4371/16300 (26.82%) | 4744/16300 (29.10%) | — | — | — | — |
| `v0.3.1` | 3845/16300 (23.59%) | 4651/16300 (28.53%) | — | — | — | — |
| `v0.2.0` | 2671/16300 (16.39%) | 2863/16300 (17.56%) | — | — | — | — |

延迟数据为引擎整句直设解码耗时：一次性设置完整拼音并读取候选，不代表逐键输入到候选窗口显示的端到端延迟。`—` 表示该版本没有按此延迟口径进行测试。完整方法见 [BENCHMARK.CN.md](BENCHMARK.CN.md)。

## 短词基准测试-65000（无上下文）
该轨道使用与下节相同的 65,000 个二至四字短词案例，但不向引擎提供光标前已上屏的文本，用于评估基础词库和无上下文短词排序。评测时关闭用户词库排序。

共同语料来源、短词案例构造、评分规则和延迟测试口径见 [BENCHMARK.CN.md](BENCHMARK.CN.md)。`Contested` 表示同一拼音在语料中对应至少两个目标词的歧义子集。

| 版本 | Top1 | Top2 | Contested Top1 | Contested Top2 | Mean (ms) | P50 (ms) | P95 (ms) | Max (ms) |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `v1.5.0` | 59088/65000 (90.90%) | 62915/65000 (96.79%) | 8335/11728 (71.07%) | 10414/11728 (88.80%) | 4.941 | 4.027 | 9.850 | 137.979 |

延迟数据为无上下文轨道的引擎单次查询耗时，不包含 TSF 调用和候选窗口渲染。

## 短词上下文基准测试-65000
该基准包含 65,000 个二至四字词的实际出现位置，每条案例都保留这个词之前已经上屏的句子前文。其案例与“长句基准测试-16300”使用同一篇小说文本作为来源，并从短词上下文模型的训练数据中排除。评测时关闭用户词库排序。

共同语料来源、短词案例构造、评分规则和延迟测试口径见 [BENCHMARK.CN.md](BENCHMARK.CN.md)。

`Contested` 表示同一拼音在语料中对应至少两个目标词的歧义子集，这部分最能反映上下文排序的作用。下表采用启用上下文的测试轨道；v1.3.0 及更早版本不会利用上下文改变短词排序，因此启用与关闭上下文的结果相同。

| 版本 | Top1 | Top2 | Contested Top1 | Contested Top2 | Mean (ms) | P50 (ms) | P95 (ms) | Max (ms) |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `v1.5.0` | 61045/65000 (93.92%) | 63364/65000 (97.48%) | 9159/11728 (78.10%) | 10677/11728 (91.04%) | 5.033 | 4.113 | 9.968 | 142.372 |
| `v1.4.0` | 60676/65000 (93.35%) | 63251/65000 (97.31%) | 8993/11728 (76.68%) | 10602/11728 (90.40%) | 5.573 | 4.521 | 11.157 | 158.687 |
| `v1.3.0` | 59078/65000 (90.89%) | 62881/65000 (96.74%) | 8326/11728 (70.99%) | 10386/11728 (88.56%) | 5.460 | 4.396 | 10.939 | 176.912 |

延迟数据为启用上下文轨道的引擎单次查询耗时，不包含 TSF 调用和候选窗口渲染。

## 配置
默认配置文件：
- `%LOCALAPPDATA%\CassotisIme\cassotis_ime.ini`

重点配置项包括：
- 拼音方案（全拼 / 微软双拼 / 小鹤双拼 / 自然码双拼 / 搜狗双拼）
- 简/繁体切换（`variant`）
- 全角 / 标点模式
- 调试日志与日志路径

运行时词库路径固定在 `%LOCALAPPDATA%\CassotisIme\data\` 下，不再通过 INI 配置。

## 文档
- 英文主文档：`README.md`
- 配置说明：`CONFIGURE.md`
- 构建说明：`BUILD.md`
- 第三方组件说明：`THIRD_PARTY.md`

## 许可
本项目采用 GPL-3.0 许可证，完整文本见 `LICENSE`。

请确保第三方声明与归属信息与 `THIRD_PARTY.md` 保持一致。

## 路线图
- 基于独立语料和 N-best 对比样本继续训练紧凑的本地重排器
- 扩充独立基准与失败归因，用于校准模型门槛和回退策略
- 强化用户词库质量控制与工具链
- 扩展编辑器/浏览器/IDE 兼容性矩阵
